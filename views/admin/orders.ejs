<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://unicons.iconscout.com/release/v4.0.0/css/line.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary: #c9a87c;
            --primary-dark: #8b6b4a;
            --bg: #f5f0e8;
            --white: #fff;
            --dark: #1a1a1a;
            --gray: #666;
            --light-gray: #e0e0e0;
        }

        body {
            font-family: 'Outfit', sans-serif;
            background: var(--bg);
            min-height: 100vh;
        }

        .sidebar {
            position: fixed;
            left: 0;
            top: 0;
            width: 260px;
            height: 100vh;
            background: var(--white);
            box-shadow: 2px 0 20px rgba(0, 0, 0, 0.05);
            padding: 1.5rem;
            z-index: 100;
        }

        .sidebar-logo {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--primary);
            text-decoration: none;
            margin-bottom: 2rem;
        }

        .sidebar-menu {
            list-style: none;
        }

        .sidebar-menu li {
            margin-bottom: 0.5rem;
        }

        .sidebar-menu a {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.875rem 1rem;
            color: var(--gray);
            text-decoration: none;
            border-radius: 10px;
            transition: all 0.3s;
        }

        .sidebar-menu a:hover,
        .sidebar-menu a.active {
            background: var(--bg);
            color: var(--primary-dark);
        }

        .sidebar-menu a.active {
            font-weight: 600;
        }

        .main-content {
            margin-left: 260px;
            padding: 2rem;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            flex-wrap: wrap;
            gap: 1rem;
        }

        .header h1 {
            font-size: 1.75rem;
            color: var(--dark);
        }

        .btn {
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 10px;
            font-size: 0.875rem;
            font-weight: 500;
            cursor: pointer;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            transition: all 0.3s;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            color: white;
        }

        .btn-secondary {
            background: var(--light-gray);
            color: var(--dark);
        }

        .btn-success {
            background: #4CAF50;
            color: white;
        }

        .btn-warning {
            background: #FF9800;
            color: white;
        }

        .btn-danger {
            background: #e74c3c;
            color: white;
        }

        .btn-sm {
            padding: 0.5rem 1rem;
            font-size: 0.8rem;
        }

        .card {
            background: var(--white);
            border-radius: 15px;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.05);
            overflow: hidden;
            margin-bottom: 1.5rem;
        }

        .card-header {
            padding: 1.5rem;
            border-bottom: 1px solid var(--light-gray);
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 1rem;
        }

        .card-header h2 {
            font-size: 1.25rem;
            color: var(--dark);
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        th,
        td {
            padding: 1rem 1.5rem;
            text-align: left;
        }

        th {
            background: var(--bg);
            font-weight: 600;
            color: var(--dark);
            font-size: 0.875rem;
        }

        td {
            border-bottom: 1px solid var(--light-gray);
            color: var(--gray);
            font-size: 0.875rem;
        }

        tr:hover td {
            background: #fafafa;
        }

        .badge {
            padding: 0.375rem 0.75rem;
            border-radius: 50px;
            font-size: 0.75rem;
            font-weight: 600;
            text-transform: capitalize;
        }

        .badge-pending {
            background: #fff3e0;
            color: #FF9800;
        }

        .badge-confirmed {
            background: #e3f2fd;
            color: #2196F3;
        }

        .badge-processing {
            background: #f3e5f5;
            color: #9C27B0;
        }

        .badge-shipped {
            background: #e0f2f1;
            color: #009688;
        }

        .badge-delivered {
            background: #e8f5e9;
            color: #4CAF50;
        }

        .badge-cancelled {
            background: #ffebee;
            color: #f44336;
        }

        .actions {
            display: flex;
            gap: 0.5rem;
            flex-wrap: wrap;
        }

        .alert {
            padding: 1rem 1.5rem;
            border-radius: 10px;
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .alert-success {
            background: #e8f5e9;
            color: #2e7d32;
        }

        .alert-error {
            background: #ffebee;
            color: #c62828;
        }

        .filter-tabs {
            display: flex;
            gap: 0.5rem;
            flex-wrap: wrap;
        }

        .filter-tabs a {
            padding: 0.5rem 1rem;
            background: var(--light-gray);
            color: var(--gray);
            text-decoration: none;
            border-radius: 50px;
            font-size: 0.875rem;
            transition: all 0.3s;
        }

        .filter-tabs a:hover,
        .filter-tabs a.active {
            background: var(--primary);
            color: white;
        }

        .order-items {
            font-size: 0.8rem;
            color: var(--gray);
        }

        .order-items li {
            margin-bottom: 0.25rem;
        }

        /* Status Select */
        .status-form {
            display: flex;
            gap: 0.5rem;
            align-items: center;
        }

        .status-form select {
            padding: 0.4rem 0.75rem;
            border: 1px solid var(--light-gray);
            border-radius: 6px;
            font-size: 0.8rem;
        }

        @media (max-width: 768px) {
            .sidebar {
                display: none;
            }

            .main-content {
                margin-left: 0;
            }
        }
    </style>
</head>

<body>
    <aside class="sidebar">
        <a href="/admin/dashboard" class="sidebar-logo"><i class="uil uil-flower"></i> Bunga Bali</a>
        <ul class="sidebar-menu">
            <li><a href="/admin/dashboard"><i class="uil uil-dashboard"></i> Dashboard</a></li>
            <li><a href="/admin/products"><i class="uil uil-box"></i> Produk</a></li>
            <li><a href="/admin/orders" class="active"><i class="uil uil-shopping-cart"></i> Pesanan</a></li>
            <li><a href="/" target="_blank"><i class="uil uil-globe"></i> Lihat Website</a></li>
            <li><a href="/admin/logout"><i class="uil uil-sign-out-alt"></i> Logout</a></li>
        </ul>
    </aside>

    <main class="main-content">
        <div class="header">
            <h1>Kelola Pesanan</h1>
            <div class="filter-tabs">
                <a href="/admin/orders" class="<%= currentStatus === 'all' ? 'active' : '' %>">Semua</a>
                <a href="/admin/orders?status=pending"
                    class="<%= currentStatus === 'pending' ? 'active' : '' %>">Pending</a>
                <a href="/admin/orders?status=confirmed"
                    class="<%= currentStatus === 'confirmed' ? 'active' : '' %>">Confirmed</a>
                <a href="/admin/orders?status=processing"
                    class="<%= currentStatus === 'processing' ? 'active' : '' %>">Processing</a>
                <a href="/admin/orders?status=shipped"
                    class="<%= currentStatus === 'shipped' ? 'active' : '' %>">Shipped</a>
                <a href="/admin/orders?status=delivered"
                    class="<%= currentStatus === 'delivered' ? 'active' : '' %>">Delivered</a>
            </div>
        </div>

        <% if (success) { %>
            <div class="alert alert-success"><i class="uil uil-check-circle"></i>
                <%= success %>
            </div>
            <% } %>
                <% if (error) { %>
                    <div class="alert alert-error"><i class="uil uil-exclamation-circle"></i>
                        <%= error %>
                    </div>
                    <% } %>

                        <div class="card">
                            <div class="card-header">
                                <h2>Daftar Pesanan (<%= orders.length %>)</h2>
                            </div>
                            <table>
                                <thead>
                                    <tr>
                                        <th>No. Pesanan</th>
                                        <th>Pelanggan</th>
                                        <th>Item</th>
                                        <th>Total</th>
                                        <th>Status</th>
                                        <th>Tanggal</th>
                                        <th>Aksi</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% if (orders.length===0) { %>
                                        <tr>
                                            <td colspan="7" style="text-align:center">Tidak ada pesanan</td>
                                        </tr>
                                        <% } else { %>
                                            <% orders.forEach(order=> { %>
                                                <tr>
                                                    <td><strong>
                                                            <%= order.order_number %>
                                                        </strong></td>
                                                    <td>
                                                        <strong>
                                                            <%= order.customer_name %>
                                                        </strong><br>
                                                        <small><i class="uil uil-phone"></i>
                                                            <%= order.customer_phone %>
                                                        </small>
                                                        <% if (order.customer_address) { %>
                                                            <br><small><i class="uil uil-map-marker"></i>
                                                                <%= order.customer_address %>
                                                            </small>
                                                            <% } %>
                                                    </td>
                                                    <td>
                                                        <ul class="order-items" style="list-style:none">
                                                            <% order.items.forEach(item=> { %>
                                                                <li>
                                                                    <%= item.quantity %>x <%= item.product_name %>
                                                                </li>
                                                                <% }) %>
                                                        </ul>
                                                    </td>
                                                    <td><strong>Rp <%= order.total_amount.toLocaleString('id-ID') %>
                                                                </strong></td>
                                                    <td><span class="badge badge-<%= order.status %>">
                                                            <%= order.status %>
                                                        </span></td>
                                                    <td>
                                                        <%= new Date(order.created_at).toLocaleDateString('id-ID', {
                                                            day: '2-digit' , month: 'short' , year: 'numeric' ,
                                                            hour: '2-digit' , minute: '2-digit' }) %>
                                                    </td>
                                                    <td>
                                                        <div class="actions">
                                                            <form action="/admin/orders/<%= order.id %>/status"
                                                                method="POST" class="status-form">
                                                                <select name="status" onchange="this.form.submit()">
                                                                    <option value="pending" <%=order.status==='pending'
                                                                        ? 'selected' : '' %>>Pending</option>
                                                                    <option value="confirmed"
                                                                        <%=order.status==='confirmed' ? 'selected' : ''
                                                                        %>>Confirmed</option>
                                                                    <option value="processing"
                                                                        <%=order.status==='processing' ? 'selected' : ''
                                                                        %>>Processing</option>
                                                                    <option value="shipped" <%=order.status==='shipped'
                                                                        ? 'selected' : '' %>>Shipped</option>
                                                                    <option value="delivered"
                                                                        <%=order.status==='delivered' ? 'selected' : ''
                                                                        %>>Delivered</option>
                                                                    <option value="cancelled"
                                                                        <%=order.status==='cancelled' ? 'selected' : ''
                                                                        %>>Cancelled</option>
                                                                </select>
                                                            </form>
                                                            <a href="https://wa.me/<%= order.customer_phone.replace(/^0/, '62').replace(/\D/g, '') %>?text=Halo%20<%= encodeURIComponent(order.customer_name) %>,%20pesanan%20Anda%20(<%= order.order_number %>)%20sedang%20kami%20proses."
                                                                target="_blank" class="btn btn-success btn-sm"
                                                                title="Hubungi via WA">
                                                                <i class="uil uil-whatsapp"></i>
                                                            </a>
                                                            <form action="/admin/orders/<%= order.id %>/delete"
                                                                method="POST" style="display:inline"
                                                                onsubmit="return confirm('Hapus pesanan ini?')">
                                                                <button type="submit" class="btn btn-danger btn-sm"><i
                                                                        class="uil uil-trash"></i></button>
                                                            </form>
                                                        </div>
                                                    </td>
                                                </tr>
                                                <% }) %>
                                                    <% } %>
                                </tbody>
                            </table>
                        </div>
    </main>
</body>

</html>